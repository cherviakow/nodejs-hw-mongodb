openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description:
    Contacts app
tags:

  - name: Contacts

  - name: Auth

servers:
  # url: http://localhost:3000
  url: https://hw6-email-and-images-m7ps.onrender.com

paths:
  /contacts/{id}:
  get:
  $ref: '../swagger/paths/contacts/{id}/get.yaml'




components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
